import { ViewStyle, TextStyle } from 'react-native'
import { color } from '../../../theme'

export const ROOT: ViewStyle = {
  flex: 1,
  backgroundColor: color.background,
}

export const notification: TextStyle = {
  flex: 1,
  textAlign: 'center',
}
